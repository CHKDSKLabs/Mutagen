# ADR-0001: Rust runtime for harness orchestration

## Context

The harness needs deterministic queue handling, structured state, and host adapters.

## Decision

Build the canonical runtime in Rust and keep host-specific behavior behind adapters.

## Consequences

The runtime gets stronger invariants and a longer shelf life at the cost of slightly slower initial implementation.
