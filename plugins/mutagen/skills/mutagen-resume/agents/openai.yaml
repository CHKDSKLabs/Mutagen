interface:
  display_name: "mutagen — resume"
  short_description: "Resume a slice after manual repair (structural-check + review dispatch in one call)"
  brand_color: "#8B1A1A"
  default_prompt: "Run $mutagen-resume after a manual repair to restart the slice from the structural-check stage."
allow_implicit_invocation: false
